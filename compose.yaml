name: ${COMPOSE_PROJECT_NAME:-govbiz-infra}

# 원본 서비스의 빌드 경로를 유지하며 두 저장소를 하나의 프로젝트로 실행합니다.
include:
  - path: ./services/SKN34-3rd-1Team/infrastructure/compose.yaml
    env_file: ${GOVBIZ_APP_ENV_FILE:-./services/SKN34-3rd-1Team/.env}
  - path: ./compose.django.yaml
    env_file: ${GOVBIZ_DJANGO_ENV_FILE:-./services/GovBiz-ops/.env}
