apiVersion: v1
kind: Service
metadata:
  name: ops-mysql
  labels:
    app.kubernetes.io/name: ops-mysql
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: ops-mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
