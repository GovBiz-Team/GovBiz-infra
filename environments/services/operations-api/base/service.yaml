apiVersion: v1
kind: Service
metadata:
  name: operations-api
  labels:
    app.kubernetes.io/name: operations-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: operations-api
  ports:
    - name: http
      port: 8000
      targetPort: http
