apiVersion: v1
kind: Service
metadata:
  name: ops-service
  labels:
    app.kubernetes.io/name: ops-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ops-service
  ports:
    - name: http
      port: 8000
      targetPort: http
